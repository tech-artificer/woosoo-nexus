#!/bin/sh
commit_regex='^(feat|fix|perf|refactor|docs|test|revert|chore|build|ci)(\(.+\))?!?: .+'
if ! grep -qE "$commit_regex" "$1"; then
  echo "Commit message must follow Conventional Commits: type(scope)?: description" >&2
  echo "Example: fix(api): handle null session" >&2
  exit 1
fi
