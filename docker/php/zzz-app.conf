; Override the official php-fpm zz-docker.conf which sets listen = /run/php/php-fpm.sock
; This file loads last (alphabetically after zz-docker.conf) to win.
; nginx → app inter-container FastCGI requires TCP, not a Unix socket.
[www]
listen = 9000
