# Contributing

## Documentation rules

- Keep active docs under `docs/`.
- Archive deprecated/historical docs under `docs/archive/`.
- Do not create root-level temporary markdown files.
- Do not introduce competing production deployment instructions.
- Update `docs/INDEX.md` when canonical docs move or are added.

Before opening a PR, satisfy `.github/pull_request_template.md`.
